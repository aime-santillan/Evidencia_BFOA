server {
    listen 80;
    server_name localhost;

    client_max_body_size 20M;

    # El root para la aplicacion Laravel
    root /var/www/html/public;
    index index.php index.html;

    # Bloque de proxy para el servidor de desarrollo Vite
    # CRITICO: Redirige las peticiones de assets al contenedor 'app' en el puerto 5173

    # 1. Redirección para el archivo /hot (usado por Vite para HMR en desarrollo)
    location = /hot {
        proxy_pass http://app:5173;
        proxy_set_header Host $host;
    }

    # 2. Redirección para los assets cargados por Vite.
    # AÑADIDO: Incluimos la ruta /resources para que Nginx redirija los archivos .js y .scss en modo dev.
    location ~ ^/(@vite|/build|/resources) {
        proxy_pass http://app:5173;

        # Configuraciones necesarias para Proxy
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Bloque principal: intenta servir el archivo, si falla, usa index.php
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Bloque para manejar archivos .php a través de PHP-FPM
    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    # Bloque para denegar acceso a archivos ocultos (como .htaccess)
    location ~ /\.ht {
        deny all;
    }
}
